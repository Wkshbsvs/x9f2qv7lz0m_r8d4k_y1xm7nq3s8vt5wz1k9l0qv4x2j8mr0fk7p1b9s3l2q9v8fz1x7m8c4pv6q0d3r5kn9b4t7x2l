local folder = "HydrogenV1.2.1"
if not isfolder(folder) then
    makefolder(folder)
end

writefile(folder .. "/.nomedia", "")

local assets = {
    ["Clear.png"] = "https://github.com/Ahma174/Assets/raw/refs/heads/main/ScriptAseets/Hydrogen%20V1.2.1/Clear.png",
    ["Close.png"] = "https://github.com/Ahma174/Assets/raw/refs/heads/main/ScriptAseets/Hydrogen%20V1.2.1/Close.png",
    ["Execute.png"] = "https://github.com/Ahma174/Assets/raw/refs/heads/main/ScriptAseets/Hydrogen%20V1.2.1/Execute.png",
    ["ExecuteClipboard.png"] = "https://github.com/Ahma174/Assets/raw/refs/heads/main/ScriptAseets/Hydrogen%20V1.2.1/Execute%20Clipboard.png",
    ["Logo.png"] = "https://github.com/Ahma174/Assets/raw/refs/heads/main/ScriptAseets/Hydrogen%20V1.2.1/Logo.png",
    ["Successful.png"] = "https://github.com/Ahma174/Assets/raw/refs/heads/main/ScriptAseets/Hydrogen%20V1.2.1/Successful.png",
    ["Editor.png"] = "https://github.com/Ahma174/Assets/raw/refs/heads/main/ScriptAseets/Hydrogen%20V1.2.1/Editor.png"
}

for name, url in pairs(assets) do
    if not isfile(folder .. "/" .. name) then
        writefile(folder .. "/" .. name, game:HttpGet(url))
    end
end

local HydrogenV121BetaGui = Instance.new("ScreenGui")
HydrogenV121BetaGui.Name = "HydrogenV1.2.1BetaGui"
HydrogenV121BetaGui.Parent = game:GetService("Players").LocalPlayer:WaitForChild("PlayerGui")
HydrogenV121BetaGui.ResetOnSpawn = false
HydrogenV121BetaGui.IgnoreGuiInset = true

local ToggleButton = Instance.new("ImageButton")
ToggleButton.Name = "ToggleButton"
ToggleButton.Size = UDim2.new(0, 52, 0, 52)
ToggleButton.Position = UDim2.new(0.5, -26, 0, 45)
ToggleButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
ToggleButton.Image = getcustomasset(folder .. "/Logo.png")
ToggleButton.ZIndex = 10
ToggleButton.Parent = HydrogenV121BetaGui

Instance.new("UICorner", ToggleButton).CornerRadius = UDim.new(1, 0)

local UIS = game:GetService("UserInputService")
local dragToggle, dragStart, startPos
local function updateInput(input)
    local delta = input.Position - dragStart
    ToggleButton.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end

ToggleButton.InputBegan:Connect(function(input)
    if (input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch) then
        dragToggle = true
        dragStart = input.Position
        startPos = ToggleButton.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragToggle = false
            end
        end)
    end
end)

ToggleButton.InputChanged:Connect(function(input)
    if (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        if dragToggle then
            updateInput(input)
        end
    end
end)

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(1, 0, 1, 0)
MainFrame.BackgroundTransparency = 1
MainFrame.Visible = false
MainFrame.Parent = HydrogenV121BetaGui

local TopBar = Instance.new("Frame")
TopBar.Name = "TopBar"
TopBar.Size = UDim2.new(0, 600, 0, 45)
TopBar.Position = UDim2.new(0.5, -300, 0, 60)
TopBar.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
TopBar.Parent = MainFrame

Instance.new("UICorner", TopBar).CornerRadius = UDim.new(0, 15)

local LogoHeader = Instance.new("ImageLabel")
LogoHeader.Size = UDim2.new(0, 70, 0, 70)
LogoHeader.Position = UDim2.new(0, -10, 0.5, -35)
LogoHeader.BackgroundTransparency = 1
LogoHeader.Image = getcustomasset(folder .. "/Logo.png")
LogoHeader.Parent = TopBar

local EditorIconHeader = Instance.new("ImageLabel")
EditorIconHeader.Size = UDim2.new(0, 32, 0, 32)
EditorIconHeader.Position = UDim2.new(0.5, -16, 0.5, -16)
EditorIconHeader.BackgroundTransparency = 1
EditorIconHeader.Image = getcustomasset(folder .. "/Editor.png")
EditorIconHeader.Parent = TopBar

local CloseBtn = Instance.new("ImageButton")
CloseBtn.Size = UDim2.new(0, 22, 0, 22)
CloseBtn.Position = UDim2.new(1, -38, 0.5, -11)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Image = getcustomasset(folder .. "/Close.png")
CloseBtn.Parent = TopBar

local EditorContainer = Instance.new("ScrollingFrame")
EditorContainer.Name = "EditorContainer"
EditorContainer.Size = UDim2.new(0, 535, 0, 260)
EditorContainer.Position = UDim2.new(0.5, -300, 0, 110)
EditorContainer.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
EditorContainer.BorderSizePixel = 0
EditorContainer.ScrollBarThickness = 4
EditorContainer.CanvasSize = UDim2.new(0, 0, 0, 0)
EditorContainer.AutomaticCanvasSize = Enum.AutomaticSize.XY
EditorContainer.ClipsDescendants = true
EditorContainer.Parent = MainFrame

Instance.new("UICorner", EditorContainer).CornerRadius = UDim.new(0, 12)

local TextBox = Instance.new("TextBox")
TextBox.Size = UDim2.new(1, -20, 1, -20)
TextBox.Position = UDim2.new(0, 10, 0, 10)
TextBox.BackgroundTransparency = 1
TextBox.Text = "-- print(\"hello, world!\")"
TextBox.ClearTextOnFocus = false
TextBox.MultiLine = true
TextBox.TextColor3 = Color3.fromRGB(200, 200, 200)
TextBox.TextXAlignment = Enum.TextXAlignment.Left
TextBox.TextYAlignment = Enum.TextYAlignment.Top
TextBox.Font = Enum.Font.Code
TextBox.TextSize = 14
TextBox.AutomaticSize = Enum.AutomaticSize.XY
TextBox.Parent = EditorContainer

local SideBar = Instance.new("Frame")
SideBar.Size = UDim2.new(0, 55, 0, 260)
SideBar.Position = UDim2.new(0.5, 245, 0, 110)
SideBar.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
SideBar.Parent = MainFrame

Instance.new("UICorner", SideBar).CornerRadius = UDim.new(0, 12)

local UIList = Instance.new("UIListLayout")
UIList.HorizontalAlignment = Enum.HorizontalAlignment.Center
UIList.VerticalAlignment = Enum.VerticalAlignment.Center
UIList.Padding = UDim.new(0, 35)
UIList.SortOrder = Enum.SortOrder.LayoutOrder
UIList.Parent = SideBar

local function AddBtn(img, order)
    local b = Instance.new("ImageButton")
    b.Size = UDim2.new(0, 22, 0, 22)
    b.BackgroundTransparency = 1
    b.Image = getcustomasset(folder .. "/" .. img)
    b.LayoutOrder = order
    b.Parent = SideBar
    return b
end

local Execute = AddBtn("Execute.png", 1)
local Clear = AddBtn("Clear.png", 2)
local ExecClip = AddBtn("ExecuteClipboard.png", 3)

ToggleButton.MouseButton1Click:Connect(function()
    if not dragToggle then
        MainFrame.Visible = true
        ToggleButton.Visible = false
    end
end)

CloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
    ToggleButton.Visible = true
end)

Clear.MouseButton1Click:Connect(function()
    TextBox.Text = ""
end)

Execute.MouseButton1Click:Connect(function()
    local success, err = pcall(function()
        loadstring(TextBox.Text)()
    end)
    if not success then
        warn("Hydrogen Error: " .. err)
    end
end)

ExecClip.MouseButton1Click:Connect(function()
    local success, err = pcall(function()
        loadstring(getclipboard())()
    end)
end)